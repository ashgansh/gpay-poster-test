```bash
# install deps
npm i


# run harhat node
cd packages/hardhat
npx hardhat node

# deploy poster onto hardhat node
npx hardhat ignition deploy ignition/modules/Poster.ts --network localhost

```
